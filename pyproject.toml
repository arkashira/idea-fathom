[project]
name = "idea-fathom"
version = "0.1.0"
description = "Minimal analytics dashboard for tracking ideas and validation metrics."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
